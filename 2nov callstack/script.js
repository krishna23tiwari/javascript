// code execution in js

// execution context

// when  we try to run js file in browser then first it created global execution context

// when we run code in browser then global execution context for browser is diffrent and 
// for not its also diffrent
// so execution context for each other is diifrent

// it works on thred and js is also single threaded language

// there are mailny 2 execution context
// 1. Global execution context
// 2. function execution context
// but there is third execution context also which is
// 3. eval execution context

